# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~
.project
.settings/
*.sublime-project
*.sublime-workspace

# Vim / Emacs backups
*.bak
*.orig
\#*\#
.\#*

# PHP / Composer (if added later)
/vendor/
composer.lock

# Node (if build tooling added later)
node_modules/
npm-debug.log*
yarn-error.log*
package-lock.json

# Environment and secrets
.env
.env.*
*.pem
*.key

# WordPress dev artifacts
wp-config.php
wp-content/uploads/
wp-content/cache/
wp-content/upgrade/
wp-content/debug.log

# Archives (build locally, do not commit)
*.zip
*.tar.gz

# PHPUnit / coverage (if tests added)
.phpunit.result.cache
/coverage/
